import styled from "styled-components";

const StyledDelivery = styled.span`
  display: block;
  font-weight: 400;
  font-size: 22px;
  line-height: 19px;
`;

const StyledValue = styled.span`
  color: green;
`;

export { StyledDelivery, StyledValue };
